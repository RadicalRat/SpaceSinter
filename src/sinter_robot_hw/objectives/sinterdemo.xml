<?xml version="1.0" encoding="UTF-8" ?>
<root BTCPP_format="4" main_tree_to_execute="SinterDemo">
  <BehaviorTree
    ID="SinterDemo"
    _description="Demo Behavior for sintering"
    _favorite="false"
  >
    <Decorator ID="Repeat" num_cycles="30">
      <Control ID="Sequence" name="TopLevelSequence">
        <SubTree
          ID="Move to Waypoint"
          _collapsed="true"
          waypoint_name="Demo_SCAN"
          joint_group_name="manipulator"
        />
        <SubTree ID="Clear Snapshot" _collapsed="true" />
        <!--Get Point cloud scan and crop. Send to UI-->
        <Action ID="WaitForDuration" delay_duration="2.00" />
        <Action
          ID="GetPointCloud"
          message_out="{point_cloud_scan}"
          topic_name="/camera/camera/depth/color/points"
          publisher_timeout_sec="5.000000"
        />
        <Action
          ID="TransformPointCloudFrame"
          input_cloud="{point_cloud_scan}"
          output_cloud="{point_cloud_scan}"
        />
        <Action
          ID="CreateStampedPose"
          stamped_pose="{crop_pose}"
          position_xyz="0;0;0.75"
          orientation_xyzw="1.000; 0.000; 0.000; 0.000"
          reference_frame="realsense_depth_optical_frame"
        />
        <Action ID="VisualizePose" pose="{crop_pose}" />
        <Action
          ID="CropPointsInBox"
          point_cloud_cropped="{point_cloud_cropped}"
          point_cloud="{point_cloud_scan}"
          crop_box_centroid_pose="{crop_pose}"
          crop_box_size="0.05;0.05;0.5;"
          _skipIf="true"
        />
        <Action
          ID="CropPointsInBox"
          point_cloud_cropped="{point_cloud_cropped}"
          point_cloud="{point_cloud_scan}"
          crop_box_centroid_pose="{crop_pose}"
          crop_box_size="0.25;0.25;2;"
        />
        <Action ID="SendPointCloudToUI" point_cloud="{point_cloud_cropped}" />
        <Action
          ID="GetCentroidFromPointCloud"
          point_cloud="{point_cloud_cropped}"
          output_pose="{centroid_pose}"
        />
        <Action
          ID="TransformPose"
          input_pose="{centroid_pose}"
          output_pose="{centroid_pose}"
          quaternion_xyzw="-0.707; 0.000; 0.000; 0.707"
          translation_xyz="0;0;0;"
        />
        <Action
          ID="VisualizePose"
          pose="{centroid_pose}"
          marker_name="centroid"
        />
        <Action
          ID="GetContourFromPointCloudSlice"
          contour_crop_angle_span="3.1415"
          contour_crop_angle_start="3.1415"
          contour_path="{contour_path}"
          input_cloud="{point_cloud_cropped}"
          max_pose_spacing="0.005"
          slice_plane="{centroid_pose}"
          slice_distance_from_plane="0.5"
          hull_alpha="-1.0"
          _skipIf="true"
        />
        <Action
          ID="GenerateCoveragePath"
          bottom_right_corner="{centroid_pose}"
          width="0.1"
          height="0.1"
          stride_distance="0.005"
          vector_of_poses="{contour_path}"
        />
        <Action
          ID="PlanCartesianPath"
          path="{contour_path}"
          tip_links="j2n6s300_focal_point"
          position_only="true"
          joint_trajectory_msg="{joint_trajectory_msg}"
          blending_radius="0.000020000"
          acceleration_scale_factor="0.25"
          velocity_scale_factor="0.25"
        />
        <Action ID="VisualizePath" path="{contour_path}" show_poses="false" />
        <Action
          ID="ExecuteTrajectory"
          joint_trajectory_msg="{joint_trajectory_msg}"
        />
      </Control>
    </Decorator>
  </BehaviorTree>
  <TreeNodesModel>
    <SubTree ID="SinterDemo">
      <MetadataFields>
        <Metadata runnable="true" />
        <Metadata subcategory="Sinter" />
      </MetadataFields>
    </SubTree>
  </TreeNodesModel>
</root>
